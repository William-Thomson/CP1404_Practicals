# Inputs
# price per kWh in cents
# daily use in kWh
# number of days in the billing period

price_per_kWh = int(input("Enter cents per kWh: "))
daily_use_per_kWh = float(input("Enter daily use in kWh: "))
number_of_days =